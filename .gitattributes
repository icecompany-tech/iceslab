# Force LF line endings for files that MUST run on Linux (shell scripts,
# Dockerfiles, systemd units, etc). Windows autocrlf would otherwise convert
# them to CRLF on checkout and they'd fail to execute on the VPS with
# `/bin/bash^M: bad interpreter`.

*.sh        text eol=lf
*.bash      text eol=lf
Dockerfile  text eol=lf
*.Dockerfile text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
*.toml      text eol=lf
Makefile    text eol=lf

# Source code: let git auto-detect, but normalize on commit.
*.ts        text
*.tsx       text
*.js        text
*.jsx       text
*.go        text eol=lf
*.json      text
*.md        text
*.css       text
*.html      text
*.sql       text

# Test goldens and fixtures are compared BYTE FOR BYTE with what the code
# renders, which is LF. Under autocrlf a Windows checkout rewrote them to CRLF,
# and every golden test failed on a render that had not changed (26.09).
**/__testdata__/**  text eol=lf

# Binary: never touch line endings.
*.png       binary
*.jpg       binary
*.jpeg      binary
*.gif       binary
*.ico       binary
*.svg       text
*.woff      binary
*.woff2     binary
*.ttf       binary
*.otf       binary
*.pdf       binary
*.zip       binary
*.tar       binary
*.gz        binary
