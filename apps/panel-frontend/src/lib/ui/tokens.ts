/**
 * Палитра панели: одно имя на один hex, на всё приложение.
 *
 * До этого файла краски объявлялись на месте: 365 констант в 40 файлах на 35
 * различных цветов. Пока имена совпадали, это была просто копипаста; беда в
 * том, что они разошлись, и разошлись тихо.
 *
 * ⚠ Главная находка инвентаря 2026-09-22, ради которой всё и сводится:
 * `WELL` и `SUNK` в разных файлах означали ОБА фона, `#0B1420` и `#08101A`, и
 * местами были поменяны местами. Замена по ИМЕНИ там перекрасила бы экран, не
 * уронив ни сборку, ни линт. Поэтому `SUNK` не переезжает сюда ни под каким
 * видом, даже как deprecated-реэкспорт: каждый его сайт переписан вручную на
 * `WELL` или `GROUND` по тому, какой hex там лежал.
 *
 * Здесь только КРАСКИ. Строки шрифтов (`DISPLAY`, `MONO`) и объекты стиля
 * (`{ fontFamily }`) сюда не кладутся: одно имя при двух формах значения это
 * отдельная ловушка, и она уже стоила разбора («Spread types may only be
 * created from object types» в дровере).
 *
 * Не входят в сведение намеренно:
 *   - `contours/profiles/.../TelegramPreview.tsx` - рисует чужой интерфейс,
 *     краски там не наши и совпадения с нашими случайны;
 *   - `app/theme.ts` - слой темы Mantine, у него своя жизнь;
 *   - фирменные краски протоколов (`EnginePicker`, `protocolTiles`);
 *   - одиночные краски контуров (по одному сайту на имя): сведение не даёт
 *     ничего, а имя теряет локальный смысл.
 */

/* ───── Фоны, от самого глубокого к самому светлому ─────────────────────── */

/** Фон страницы, самый глубокий слой. */
export const GROUND = '#08101A';
/** Утопленный блок внутри карточки: поле, строка списка, раскрытый ответ. */
export const WELL = '#0B1420';
/** Карточка на фоне страницы. */
export const CARD = '#0F1A28';
/** Приподнятая строка внутри карточки: ряд таблицы, бейдж, чип. */
export const ROW = '#152233';

/* ───── Линии ───────────────────────────────────────────────────────────── */

/** Граница карточки и разделитель. */
export const HAIRLINE = '#1C2A3D';
/** Граница поля ввода и пунктир «ещё не настоящее». */
export const EDGE = '#2C3A4E';

/* ───── Текст, от самого яркого к самому тихому ─────────────────────────── */

/** Основной текст. */
export const SNOW = '#C8D4E3';
/** Подпись, пояснение, второстепенное значение. */
export const MIST = '#7A8BA3';
/** Третий уровень: служебные пометки рядом с основным текстом. */
export const FAINT = '#5A6B82';
/** Самый тихий: placeholder, выключенное, «ничего нет». */
export const DIM = '#3A4A60';

/* ───── Состояния и акценты ─────────────────────────────────────────────── */

/** Акцент панели: ссылки, активное, выбранное. */
export const CYAN = '#7DD3FC';
/** Тот же акцент на шаг ярче, для наведения и выделенного числа. */
export const CYAN_HI = '#67E8F9';
/** Хорошо: применено, онлайн, свободно. */
export const MOSS = '#A7D8B9';
/** Внимание: ждём, устарело, применится не везде. */
export const AMBER = '#F5B14C';
/** Плохо: отказ, недоступно, занято. */
export const RED = '#E07A5F';
/** Второй акцент: каскады, балансер, всё, что не про состояние. */
export const VIOLET = '#A78BFA';
/** Знак бренда. */
export const ROSE = '#E08AA8';

/* ───── Старых имён здесь больше нет ────────────────────────────────────────
 * На время переезда файл держал девять реэкспортов, чтобы замена шла контур за
 * контуром: SLATE, DIM_TEXT, STALE, CLAY, BORDER_INPUT, HOVER, RAISED, BADGE,
 * CYAN2. Все девять сняты 2026-09-22, когда у каждого стало ноль импортёров.
 *
 * Правило на будущее: deprecated-имя здесь это СПИСОК РАБОТЫ, а не архив. Не
 * переживает волну, в которой заведено.
 *
 * `SUNK` в этом списке не было никогда: он означал два разных фона, см.
 * предупреждение в шапке, и каждый его сайт переписан вручную.
 */
