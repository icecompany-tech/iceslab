/**
 * Палитра контура сквадов.
 *
 * Краски приходят из `lib/ui/tokens.ts`: одно имя на один цвет на всю панель.
 * Своих красок у контура нет ни одной, оба его экрана до этого держали по
 * собственной копии общей палитры.
 */
export {
  HAIRLINE,
  CARD,
  WELL,
  GROUND,
  ROW,
  SNOW,
  MIST,
  FAINT,
  DIM,
  CYAN,
  CYAN_HI,
  MOSS,
  AMBER,
  VIOLET,
} from '@/lib/ui/tokens';
